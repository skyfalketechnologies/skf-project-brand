# Local dev — Vite proxy can use /api; production set your Render URL
VITE_API_URL=http://127.0.0.1:8000/api

# Production example (Netlify / Vercel env vars):
# VITE_API_URL=https://your-service-name.onrender.com/api
